[pytest]
DJANGO_SETTINGS_MODULE = scifi_zone.settings
python_files = test_*.py *_tests.py
addopts = -p no:warnings --strict-markers --no-migrations --reuse-db
markers =
    new
    custom_model
    custom_view
    custom_form
